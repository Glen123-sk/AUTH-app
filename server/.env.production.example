NODE_ENV=production
PORT=5000
TRUST_PROXY=true
USE_GITHUB_AUTH_ONLY=true

# Replace placeholders with your MongoDB Atlas credentials
MONGO_URI=mongodb+srv://<db_user>:<db_password>@<cluster-host>/auth_smtp_app?retryWrites=true&w=majority&appName=auth-smtp-app
# URL-encode special characters in the password before setting MONGO_URI.
# Add authSource=<db> only if your database user was created outside the database named in the URI path.
MONGO_CONNECT_RETRIES=8
MONGO_CONNECT_RETRY_DELAY_MS=3000
MONGO_SERVER_SELECTION_TIMEOUT_MS=10000

JWT_SECRET=replace_with_very_long_random_secret
JWT_EXPIRES_IN=1d
RESET_TOKEN_EXPIRES_IN=10m
SESSION_EXPIRES_IN_DAYS=30
SESSION_COOKIE_SECURE=auto
SESSION_COOKIE_SAME_SITE=lax

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_REQUIRE_TLS=true
SMTP_REJECT_UNAUTHORIZED=true
SMTP_CONNECTION_TIMEOUT_MS=10000
SMTP_GREETING_TIMEOUT_MS=10000
SMTP_SOCKET_TIMEOUT_MS=20000
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="Auth App <no-reply@example.com>"

# GitHub OAuth (required when USE_GITHUB_AUTH_ONLY=true)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_CALLBACK_URL=https://your-live-domain.com/auth/github/callback
GITHUB_FILE_DB_PATH=

# Use your production frontend domain(s), comma-separated
CORS_ORIGIN=https://nexl.me,https://www.nexl.me
